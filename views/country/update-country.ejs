<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" type="text/css" href="/css/country-details.css" />
  <title>Update Country</title>
</head>
<body class="add-country-page">

  <%- include('../partials/navbar') %>

  <div class="container">
    <h1>Country Update</h1>
    <p class="subtitle">Edit the country details below and save your changes.</p>

    <% const selected = foundCountry.season%>
    <form action="/country/<%= foundCountry._id%>/update?_method=PUT" method="post">

    <div class="card">
      <div class="field">
        <span class="label">Country</span>
        <div class="value">
          <input value="<%= foundCountry.name %>" type="text" name="name" id="name" form="updateForm" />
        </div>
      </div>

      <div class="field">
        <span class="label">Notes</span>
        <div class="value">
          <textarea value="<%=foundCountry.notes%>" name="notes" id="notes"><%=foundCountry.notes%></textarea>
        </div>
      </div>

      <div class="field">
        <span class="label">Travel Season</span>
        <div class="value">
        <select name="season" required value >
            <option value="select">--Select a season--</option>
            <option value="spring"<%= selected === "Spring" && "selected" %>>Spring</option>
            <option value="summer"<%= selected === "Summer" && "selected" %>>Summer</option>
            <option value="autumn"<%= selected === "Autumn" && "selected" %>>Autumn</option>
            <option value="Winter"<%= selected === "Winter" && "selected" %>>Winter</option>
        </select>
        <br>
        </div>
      </div>

      <div class="actions">
       
         <button type="submit"  class="btn btn-edit">Update Country</button>

      </div>
    </div>
    </form>
  </div>
</body>
</html>























 